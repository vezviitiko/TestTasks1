###2. Напишите функцию, которая на вход принимает
# единственное целое число N, а возвращает целый
# квадратный корень из этого числа, если такой существует,
# или None, если такого корня нет. Нельзя использовать
# функцию sqrt() из модуля math для извлечения квадратного корня.

def my_sqrt(N):
    sqrt = N**(1/2)
    if sqrt.is_integer():
        return int(sqrt)
    else:
        return None

b = my_sqrt(25)
print(b)
b = my_sqrt(20)
print(b)
